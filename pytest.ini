[pytest]
testpaths = tests
# Live tests hit real upstream endpoints and are excluded by default, so the
# suite stays offline, fast and deterministic. Run them with: pytest -m live
addopts = -m "not live"
markers =
    live: hits real Microsoft / anaconda.org endpoints (opt in with -m live)
